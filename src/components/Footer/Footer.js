'use client';
import { Box } from '@mui/material';

const Footer = () => {
  return(
    <Box
      sx={{
        height: '200px',
        backgroundColor: 'red'
      }}
    >
      Footer
    </Box>
  )
};

export default Footer;
